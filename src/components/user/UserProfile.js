

const UserProfile = ({name, surname}) => {
    console.log('UserProfile component is rendering')
    return (
        <div>Name: {name} and Surname: {surname}</div>
    )
}

export default UserProfile